import React, { useState, useEffect } from 'react';

export default function ShowDocs(id) {

    return (
        <div>
        {/* <h1>{body.title}</h1> */}
        {/* <h1>{body.component}</h1> */}
        <h1>{id}</h1>
        </div>
    );
}
